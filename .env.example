# ------------------------------------------------------------------
# ROOT MONOREPO EXAMPLE
# Use this file for local development when web + api + worker run together.
# ------------------------------------------------------------------

# ------------------------------------------------------------------
# SHARED INFRASTRUCTURE
# Required by both API and worker.
# ------------------------------------------------------------------
RABBITMQ_URL=amqp://guest:guest@localhost:5672
RABBITMQ_QUEUE_NAME=crawlix.scrape.jobs
RABBITMQ_RETRY_QUEUE_NAME=crawlix.scrape.jobs.retry
RABBITMQ_DLQ_QUEUE_NAME=crawlix.scrape.jobs.dlq
RABBITMQ_RETRY_DELAY_MS=15000
RABBITMQ_MAX_DELIVERY_ATTEMPTS=3
REDIS_URL=redis://localhost:6379
REDIS_JOB_PREFIX=crawlix:jobs
RESULT_TTL_SECONDS=3600

# ------------------------------------------------------------------
# API SERVICE
# Used by apps/api.
# ------------------------------------------------------------------
API_PORT=3001
# Fallback legacy port variable still supported by api/worker/web scripts.
# PORT=3001
API_SERVICE_NAME=crawlix-api
CORS_ORIGIN=http://localhost:3000
DASHBOARD_ORIGIN=http://localhost:3000
PUBLIC_API_RATE_LIMIT_PER_MINUTE=60

# ------------------------------------------------------------------
# DASHBOARD AUTH
# Used by apps/api for admin session auth.
# ------------------------------------------------------------------
SESSION_SECRET=replace-with-strong-session-secret
SESSION_COOKIE_NAME=crawlix_session
SESSION_TTL_SECONDS=43200
DASHBOARD_ADMIN_EMAIL=admin@crawlix.local
DASHBOARD_ADMIN_PASSWORD=replace-with-strong-admin-password
API_KEY_PREFIX=cx

# ------------------------------------------------------------------
# WORKER SERVICE
# Used by apps/worker.
# ------------------------------------------------------------------
WORKER_PORT=3002
WORKER_SERVICE_NAME=crawlix-worker

# ------------------------------------------------------------------
# SCRAPER RUNTIME
# Shared by API defaults and worker execution behavior.
# ------------------------------------------------------------------
SCRAPER_DEFAULT_STRATEGY=auto
SCRAPER_TIMEOUT_MS=30000
SCRAPER_CACHE_TTL_SECONDS=900
SCRAPER_MAX_RETRIES=2
SCRAPER_RETRY_DELAY_MS=1000
SCRAPER_PROXY_URL=
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_EXECUTABLE_PATH=
PLAYWRIGHT_BROWSER_IDLE_TIMEOUT_MS=120000

# ------------------------------------------------------------------
# WEB APP
# Used by apps/web.
# ------------------------------------------------------------------
DASHBOARD_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001/api
